## 🚨 重要提示

> # ⚠️ 每个 PR 应专注于单一目的
> 
> **如果此 PR 涉及多个不相关的变更，AI 应拒绝生成 PR 标题和描述，并建议开发者将其拆分为多个较小的 PR。**
>
> **AI 注意**：模板中所有以`> **XXXX**` 等开头的说明性内容都是给 AI 的指令，不应包含在最终生成的 PR 标题和描述中。

# PR 标题

**格式**：`[类型] 简短描述 [LINEAR-xxx]`

**类型**：[plan|feat|fix|refactor|infra|data|perf|chore|test|docs|build|arch]

> **类型说明**：
> - `plan`: 新plan doc
> - `feat`: 新功能（App 用户可感知的功能——如果用户看不到变化，不是 feat）
> - `fix`: 错误修复
> - `refactor`: 代码重构
> - `data`: 数据相关更改
> - `perf`: 性能优化
> - `infra`: 开发基础设施（脚本、工具链、ralph、ui-test 框架、内部 KMP 模块如 logger-service / build-info-service 等非用户可见的代码）
> - `chore`: 清理旧实验、旧flag、已弃用功能等
> - `test`: 测试相关
> - `docs`: 文档相关
> - `build`: 构建脚本、CI/CD 等
> - `arch`: 架构相关更改

**示例**：
- `[feat] 添加facebook登陆功能 [MOB-123]`
- `[fix] 修复 iOS 图片下载失败问题 [MOB-456]`
- `[refactor] 重构选择逻辑，合并 selectSingleElement 和 selectMultipleElements [MOB-789]`

---

# PR 描述

## 摘要
- **PR 目的**：[说明此 PR 的目的，可以是业务相关（如：改善注册流程转化率）或工程相关（如：重构选择逻辑）]
- **主要变更**：[描述此 PR 的主要变更]
- **大小**：[XS|S|M|L|XL|XXL|XXXL]

> **大小说明**：
> - `XS`: 超小任务（代码变更行数 < 50，涉及文件数 < 3，新增抽象数 0）
> - `S`: 小任务（代码变更行数 50-200，涉及文件数 3-5，新增抽象数 0-1）
> - `M`: 中等任务（代码变更行数 200-500，涉及文件数 5-10，新增抽象数 1-3）
> - `L`: 大任务（代码变更行数 500-1000，涉及文件数 10-20，新增抽象数 3-5）
> - `XL`: 超大任务（代码变更行数 > 1000，涉及文件数 > 20，新增抽象数 > 5）
> - `XXL`: 特大任务（代码变更行数 > 3000，涉及文件数 > 50，跨多个模块的架构变更）— 适合由 AI Agent 主导执行、有完整单元测试和 UI 测试覆盖的场景
> - `XXXL`: 巨型任务（代码变更行数 > 5000，涉及文件数 > 100，系统级架构重构）— 仅适用于 AI Agent 全程执行 + 完整自动化测试套件可兜底的 yolo 场景
>
> **机械性变更降级规则**：
> 上述大小阈值衡量的是**设计/审查复杂度**，不是 diff 行数或文件数。如果 PR 绝大部分变更是纯机械操作——每处都遵循同一条可机械验证的规则、不涉及业务逻辑或设计判断——应将大小**至少降一档，必要时大幅降级**（例如：100 文件的方法重命名 PR 实际复杂度可能只是 XS/S，因为审查者抽样 3-5 处确认替换规则一致即可，不需要逐文件思考）。
>
> 典型机械性变更：
> - 重命名 public 方法 / 类 / 字段，导致全仓库 N 个调用点跟改
> - 修改某个广泛使用的函数签名（增删参数、改返回类型），所有调用点机械跟改
> - 模块拆分 / 重组，大量文件移动 + import 路径调整
> - 批量修复新增 lint 规则触发的全仓库违规（参考 PR #332）
> - 按 codemod 规则批量替换 API（旧 API → 新 API 迁移）
> - 统一 import 顺序 / 路径 / 别名
> - 目录重组、按规范批量重命名文件
> - 给已有未标注的代码批量加类型注解
>
> 判断准则：
> - 审查者是否需要逐文件思考？如果只需抽样核对"是否都按同一规则改"，就是机械性变更
> - 仍按原始复杂度计大小的部分：触发机械变更的"源头"本身（新增的 lint 规则、codemod 脚本、新 API 接口、新签名的方法声明）不参与降级

- **计划文档**：[计划文档路径，如 `plans/xxx.md`]

> **计划文档说明**：
> - XS/S 大小的 PR：计划文档**可选**
> - M/L/XL/XXL/XXXL 大小的 PR：计划文档**必填**，且必须是仓库内的文件路径（如 `plans/xxx.md`）
- **产品设计文档（如适用）**：[产品设计文档路径]
- **PR Train（如适用）**：
    - **前置 PR**：[#pr-number]
    - **后续 PR**：[#pr-number]

## 变更内容
> **重要**：这些变更项应汇总此 PR 中所有提交的标题和描述。

<!-- 以下为示例：-->
- 变更项 1：[描述]
- 变更项 2：[描述]
- 变更项 3：[描述]
<!-- 示例结束 -->

## 测试

### 手动测试 （如适用）
> **重要**：测试场景应从计划文档中提取。
> **重要**：如果此 PR 包含任何 UI 变更，建议为每个测试场景上传 GIF 格式的屏幕录制。

<!-- 以下为示例：-->
- [ ] **测试场景 1**：[场景描述]
- [ ] **测试场景 2**：[场景描述]
- [ ] **测试场景 3**：[场景描述]
<!-- 示例结束 -->

## 截图/GIF（如适用）
如果此 PR 涉及 UI 变更，建议为每个测试场景上传 GIF 格式的屏幕录制：
<!-- 在此处添加 GIF 链接或直接上传 GIF -->

## 检查清单
> **注意**：检查清单中的所有项目必须保持未勾选状态（`[ ]`），由开发者在合并前手动勾选。

在 PR 合并前，请确认：
- [ ] 所有手动测试已通过（如适用）
- [ ] 开发者已审查所有新增和修改的文件
- [ ] 如果此 PR 包含任何 UI 变更，必须至少有一张截图或 GIF 格式的录屏
- [ ] iOS 应用仍能编译并运行（启动后不立即崩溃）
- [ ] Android 应用仍能编译并运行（启动后不立即崩溃）

## 其他说明
如有其他需要说明的内容，请在此处添加。
